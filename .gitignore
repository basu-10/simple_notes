# ── Dependencies (never commit) ──
node_modules/

# ── Capacitor generated web assets ──
# The repo ROOT holds the canonical web source (app.html, css/, js/, …).
# www/ is regenerated from it via: npm run build:www
www/

# ── Android / Gradle build artifacts ──
android/.gradle/
android/**/build/
android/local.properties
android/captures/
android/app/src/main/assets/
android/capacitor-cordova-android-plugins/
android/.idea/
*.keystore
*.jks


# ── App artifacts ──
js/vendor


# ── Editor / IDE noise ──
.vscode/
.idea/
*.swp
*~
*.swo

# ── OS noise ──
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# ── Secrets / environment ──
.env
.env.*
*.local

# ── Logs ──
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── Staged Android APK (served by the web deploy, not committed) ──
downloads/*.apk


tests
test-results/